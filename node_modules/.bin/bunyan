#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../bunyan-sfdx-no-dtrace/bin/bunyan" "$@"
  ret=$?
else 
  node  "$basedir/../bunyan-sfdx-no-dtrace/bin/bunyan" "$@"
  ret=$?
fi
exit $ret
