/*
 * Copyright (c) 2016, salesforce.com, inc.
 * All rights reserved.
 * Licensed under the BSD 3-Clause license.
 * For full license text, see LICENSE.txt file in the repo root  or https://opensource.org/licenses/BSD-3-Clause
 */
/* eslint global-require: 0 */
'use strict';
const path = require('path');
const messages = require(path.join(__dirname, '..', '..', 'lib', 'messages'))();
const consts = require(path.join(__dirname, '..', '..', 'lib', 'constants'));
module.exports = function (_execCommand) {
    return {
        description: messages.getMessage('cliDescription', [], 'package2_version_get'),
        longDescription: messages.getMessage('cliLongDescription', [], 'package2_version_get'),
        help: messages.getMessage('help', [], 'package2_version_get'),
        orgType: consts.DEFAULT_DEV_HUB_USERNAME,
        supportsTargetDevHubUsername: true,
        flags: [
            {
                name: 'package2versionid',
                char: 'i',
                description: messages.getMessage('id', [], 'package2_version_update'),
                longDescription: messages.getMessage('idLong', [], 'package2_version_update'),
                hasValue: true,
                required: true,
                type: 'id'
            }
        ],
        /**
         * the heroku cli hook
         * @param context - the cli context
         * @returns {Promise}
         */
        run(context) {
            const Package2VersionGetCommand = require(path.join('..', '..', 'lib', 'package2', 'package2VersionGetCommand'));
            return _execCommand(new Package2VersionGetCommand(), context);
        }
    };
};

//# sourceMappingURL=package2_version_get.js.map
