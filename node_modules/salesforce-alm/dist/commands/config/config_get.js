/* eslint global-require: 0 */
'use strict';
// Node
const path = require('path');
// Local
const messages = require(path.join(__dirname, '..', '..', 'lib', 'messages'))();
module.exports = function (_execCommand) {
    return {
        description: messages.getMessage('description', [], 'configGetCommand'),
        longDescription: messages.getMessage('descriptionLong', [], 'configGetCommand'),
        help: messages.getMessage('help', [], 'configGetCommand'),
        requiresProject: false,
        variableArgs: true,
        flags: [{
                name: 'verbose',
                hidden: false
            }],
        /**
         * the heroku cli hook
         * @param context - the cli context
         * @returns {*  }
         */
        run(context) {
            const ConfigGetCommand = require(path.join(__dirname, '..', '..', 'lib', 'config', 'ConfigGetCommand'));
            return _execCommand(new ConfigGetCommand(), context);
        }
    };
};

//# sourceMappingURL=config_get.js.map
