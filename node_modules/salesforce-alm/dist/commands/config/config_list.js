/* eslint global-require: 0 */
'use strict';
// Node
const path = require('path');
// Local
const messages = require(path.join(__dirname, '..', '..', 'lib', 'messages'))();
module.exports = function (_execCommand) {
    return {
        description: messages.getMessage('description', [], 'configListCommand'),
        longDescription: messages.getMessage('descriptionLong', [], 'configListCommand'),
        help: messages.getMessage('help', [], 'configListCommand'),
        requiresProject: false,
        /**
         * the heroku cli hook
         * @param context - the cli context
         * @returns {*  }
         */
        run(context) {
            const ConfigListCommand = require(path.join(__dirname, '..', '..', 'lib', 'config', 'ConfigListCommand'));
            return _execCommand(new ConfigListCommand(), context);
        }
    };
};

//# sourceMappingURL=config_list.js.map
